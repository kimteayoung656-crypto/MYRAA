<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_myraa_gradient">

    <!-- Character stage: swap this ImageView's src for your own companion art asset -->
    <ImageView
        android:id="@+id/imgCharacter"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="24dp"
        android:contentDescription="@null"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/micButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Status / listening prompt text -->
    <TextView
        android:id="@+id/txtPrompt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="140dp"
        android:text="@string/idle_prompt"
        android:textAllCaps="true"
        android:textColor="@color/myraa_text_muted"
        android:letterSpacing="0.08"
        android:textSize="15sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Core Error Protocol card, hidden until a capture/permission error occurs -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/errorCard"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:background="@drawable/bg_error_card"
        android:padding="16dp"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@id/micButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.7">

        <TextView
            android:id="@+id/txtErrorTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="@string/core_error_title"
            android:textColor="@color/myraa_error_accent"
            android:textStyle="bold"
            android:textAllCaps="true"
            android:letterSpacing="0.05"
            android:textSize="14sp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <TextView
            android:id="@+id/txtErrorMessage"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="@string/capture_error_message"
            android:textColor="@color/white"
            android:textSize="14sp"
            app:layout_constraintTop_toBottomOf="@id/txtErrorTitle"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <TextView
            android:id="@+id/txtDismiss"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@string/dismiss_code"
            android:textColor="@color/myraa_error_accent"
            android:textStyle="bold"
            android:textSize="12sp"
            android:letterSpacing="0.05"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            app:layout_constraintTop_toBottomOf="@id/txtErrorMessage"
            app:layout_constraintStart_toStartOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Mic button -->
    <ImageButton
        android:id="@+id/micButton"
        android:layout_width="76dp"
        android:layout_height="76dp"
        android:layout_marginBottom="48dp"
        android:background="@drawable/bg_mic_ring"
        android:src="@drawable/ic_mic"
        android:contentDescription="@string/idle_prompt"
        android:padding="22dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Close / dismiss session -->
    <ImageButton
        android:id="@+id/closeButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_close"
        android:contentDescription="@string/dismiss_code"
        app:layout_constraintBottom_toBottomOf="@id/micButton"
        app:layout_constraintTop_toTopOf="@id/micButton"
        app:layout_constraintStart_toEndOf="@id/micButton"
        android:layout_marginStart="24dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
